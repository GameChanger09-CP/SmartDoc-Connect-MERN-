source venv/bin/activate -> activate the virtual env ... 

sudo -u postgres psql -c "DROP DATABASE smartdoc_db;" -> delete old database 

sudo -u postgres psql -c "CREATE DATABASE smartdoc_db;" -> CREATE new database 

find . -path "*/migrations/*.py" -not -name "__init__.py" -delete
find . -path "*/migrations/*.pyc"  -delete
-> delete history files ... 

python manage.py makemigrations
python manage.py migrate 
  -> Scans your models.py file and writes new instructions on how to build the database tables (Users, Documents, Departments).
 -> Apply migrations ... 

python manage.py createsuperuser -> createsuperuser creates main admin physical entry ... 


after that this will be not needed ... 
python manage.py shell -c "from api.models import User; u = User.objects.get(username='admin'); u.role = 'Main_Admin'; u.kyc_status = 'Verified'; u.save(); print('Admin Role Fixed!')" -> fix main admin ... 

python manage.py runserver -> run the backend ... 



FRONTEND : 

rm -rf node_modules/.vite -> delete temporary styling files to ensure new UI colors load correctly ... 

npm run dev -> run the frontend ... 


is_superuser -> stting .... 
python manage.py createsuperuser
python manage.py shell -c "from api.models import User; u = User.objects.get(username='admin'); u.role='Main_Admin'; u.is_superuser=True; u.is_staff=True; u.save(); print('ADMIN FIXED')"


missing migrations : 

python manage.py makemigrations
python manage.py migrate
python manage.py shell
from api.models import User
from rest_framework.authtoken.models import Token

# 1. Get or Create User
user, created = User.objects.get_or_create(username='admin', defaults={'email': 'admin@example.com'})

# 2. Set details
user.set_password('123')
user.role = 'Main_Admin'
user.is_superuser = True
user.is_staff = True
user.kyc_status = 'Verified'
user.save()

# 3. Ensure Token Exists (This was the part crashing before!)
Token.objects.get_or_create(user=user)

print("-" * 30)
print("SUCCESS: Admin fixed. Database is healthy.")
print("Login with -> User: admin | Pass: 123")
print("-" * 30)